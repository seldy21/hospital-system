import SigninContainer from '@/components/Signin/SigninContainer';

export const metadata = {
  title: '회원가입',
  description: '사암당 한의원 회원가입 페이지입니다.',
};

export default function SignInPage() {
  return <SigninContainer />;
}
